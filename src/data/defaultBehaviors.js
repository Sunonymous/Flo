export const defaultBehaviors = "Acknowledge\nProbing Questions\nWhat Else?\nAccount Audit\nNBA\nRecap";
export const defaultBehaviorsArray = defaultBehaviors.split("\n");

export default {
    string: defaultBehaviors,
     array: defaultBehaviorsArray,
}